/**
 * Two steps of the same blue, alternated row by row across the horizontal bar
 * charts. Both clear the 2:1 contrast floor against the white card, and the
 * pair is separated enough in lightness to read as two distinct shades.
 *
 * The alternation is rhythm, not encoding — bar length carries the value — so
 * these charts deliberately carry no colour legend.
 */
export const BAR_SHADES = ['var(--color-series)', 'var(--color-series-light)']
